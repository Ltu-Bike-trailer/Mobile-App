// fetches last row of table: "SELECT * FROM TableName ORDER BY rowid DESC LIMIT 1;"
// 
// When bluetooth sends data, parse data, and insert it into localtable. When table is updated fetches last row of table for values on the display